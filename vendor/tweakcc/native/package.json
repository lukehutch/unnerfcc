{
  "name": "@unnerfcc/native-io",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vendored native-binary I/O for unnerfcc: extract and repack the JS bundle of a Claude Code Bun standalone binary. Copied from tweakcc-fixed (see UPSTREAM.md).",
  "bin": {
    "unnerfcc-native-io": "./dist/cli.mjs"
  },
  "scripts": {
    "build": "esbuild cli.mjs --bundle --platform=node --format=esm --outfile=dist/cli.mjs --external:node-lief"
  },
  "dependencies": {
    "node-lief": "^1.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
