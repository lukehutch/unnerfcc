{
  "name": "@unnerfcc/prompt-patcher",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vendored system-prompt patcher for unnerfcc: splice edited system-prompt .md files into a Claude Code JS bundle using a local prompts-<version>.json catalog. Copied from tweakcc-fixed (see UPSTREAM.md).",
  "bin": {
    "unnerfcc-prompt-patcher": "./dist/cli.mjs"
  },
  "scripts": {
    "build": "esbuild cli.mjs --bundle --platform=node --format=esm --outfile=dist/cli.mjs --banner:js=\"import{createRequire as __ccreq}from'node:module';const require=__ccreq(import.meta.url);\""
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
