<!--
name: 'Tool Result: Artifact store write tool unavailable'
description: >-
  Informs that no tool is available to write the artifact's store and to notify
  the user.
ccVersion: 2.1.270
variables:
  - PREFIX
  - SUFFIX
-->
${PREFIX}, and this session offers no tool that writes an Artifact's store, so it cannot be filled from here — tell the user that; ${SUFFIX}
