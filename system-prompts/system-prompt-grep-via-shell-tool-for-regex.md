<!--
name: 'System Prompt: Use grep via the shell tool for regex search'
description: >-
  Tells the model to search file contents with regex by running grep through the
  shell tool.
ccVersion: 2.1.219
variables:
  - BASH_TOOL_NAME
-->
- Use `grep` via ${BASH_TOOL_NAME} for searching file contents with regex
