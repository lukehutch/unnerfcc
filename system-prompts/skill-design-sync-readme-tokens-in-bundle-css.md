<!--
name: 'Skill: Design sync README tokens in bundle CSS'
description: Documentation note that design tokens are declared inside _ds_bundle.css.
ccVersion: 2.1.251
-->
They are declared inside `_ds_bundle.css` (this DS ships one compiled stylesheet rather than separate token files).
