<!--
name: 'System Prompt: Use find via Bash for broad file patterns'
description: >-
  Tool-selection bullet telling the model to run `find` through the shell tool
  for broad file pattern matching.
ccVersion: 2.1.219
variables:
  - BASH_TOOL_NAME
-->
- Use `find` via ${BASH_TOOL_NAME} for broad file pattern matching
