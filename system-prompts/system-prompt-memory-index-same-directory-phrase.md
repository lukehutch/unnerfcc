<!--
name: 'System Prompt: Memory index in that same directory'
description: >-
  Names the index a memory pointer goes into — the index file sitting in the
  same directory as the memory files.
ccVersion: 2.1.231
variables:
  - MEMORY_INDEX_FILENAME
-->
the `${MEMORY_INDEX_FILENAME}` in that same directory
