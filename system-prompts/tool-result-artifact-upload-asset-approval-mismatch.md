<!--
name: 'Tool Result: upload_asset approval files mismatch'
description: >-
  Explains that the call arguments no longer match the approved file list and
  asks to retry for re-approval.
ccVersion: 2.1.277
-->
the call no longer names the files that were approved (one file where a list was approved, a list of another length, or a file whose identity was not recorded) — retry so it is checked again
