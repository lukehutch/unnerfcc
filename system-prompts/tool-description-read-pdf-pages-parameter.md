<!--
name: 'Tool Description: Read PDF pages parameter'
description: >-
  Read tool note that PDFs are read through the `pages` parameter, with a
  per-request page cap and a requirement to pass it for long PDFs.
ccVersion: 2.1.219
-->
 Reads PDFs via the `pages` parameter (e.g. "1-5", max 20 pages/request; required for PDFs over 10 pages).
