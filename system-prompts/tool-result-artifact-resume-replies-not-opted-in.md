<!--
name: 'Tool Result: Auto-replies not resumed — session never opted in'
description: >-
  Tells the model this session never opted into automatic comment replies so a
  resume has nothing to re-enable, the stop was left as it was, and it should
  not retry here.
ccVersion: 2.1.235
-->
Auto-replies were NOT resumed: this session has not opted into automatic comment replies, so a resume has nothing it could re-enable. The stop was left as it was; do not retry in this session.
