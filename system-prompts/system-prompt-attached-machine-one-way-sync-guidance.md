<!--
name: 'System Prompt: Attached machine one-way sync guidance'
description: >-
  Guidance for sessions with one-way sync on where to make persistent changes
  versus where to read and search.
ccVersion: 2.1.263
variables:
  - ACTION_DESCRIPTION
  - SYNC_TIMING_NOTE
  - ARGUMENT_NAME
-->
${ACTION_DESCRIPTION} — make project changes the user should keep that way (edits to this session's copy are not sent back; ${SYNC_TIMING_NOTE}); read and search the project in this session's copy, without "${ARGUMENT_NAME}"
