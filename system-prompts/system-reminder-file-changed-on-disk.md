<!--
name: 'System Reminder: File changed on disk since you last read it'
description: >-
  Shared opening of the file-change notice — the file changed since the last
  read, that is usually deliberate, so take it as the current state and say so
  rather than undoing it.
ccVersion: 2.1.235
-->
 changed on disk since you last read it. That's usually deliberate, so take it as the current state rather than reverting it; if the change looks wrong, say so rather than undoing it yourself — otherwise no need to call it out.
