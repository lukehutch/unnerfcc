<!--
name: 'Tool Result: read_db token calls parameters detail'
description: >-
  Provides the exact collection and doc_id parameters needed to fetch design
  system tokens via read_db.
ccVersion: 2.1.272
variables:
  - ARTIFACT_URL
-->
, `url`: ${ARTIFACT_URL}, once with `collection`: "api", `doc_id`: "tokens.md" and once with `collection`: "ds", `doc_id`: "tokens" — since the type's instructions will ask for them.
