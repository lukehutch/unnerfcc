<!--
name: 'Skill: No project language detected'
description: >-
  Tells the model no project language was auto-detected, so it should ask the
  user which language they are using and then Read the matching language README
  — or the cURL examples for raw HTTP or an unsupported language — from the base
  directory before anything else.
ccVersion: 2.1.235
-->
No project language was auto-detected. Ask the user which language they are using (see Language Detection above), then Read the matching `{lang}/claude-api/README.md` (or `curl/examples.md` for cURL/raw HTTP or an unsupported language) from the base directory before anything else.
