<!--
name: 'Tool Result: Plugin eval init run inside session notice'
description: >-
  Explains that claude plugin eval init was run inside a Claude Code session
  where no separate terminal interview can run.
ccVersion: 2.1.270
-->
`claude plugin eval init` was run from inside a Claude Code session, so no separate interview was started (there is no terminal here to host one).
