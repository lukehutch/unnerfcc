<!--
name: 'Tool Description: CronCreate (concise)'
description: >-
  Concise CronCreate description — schedule a prompt to run on a cron schedule
  or once, with `durable: true` persisting it beyond the session.
ccVersion: 2.1.219
-->
Schedule a prompt to run at a future time — either recurring on a cron schedule, or once at a specific time. Pass durable: true to persist to .claude/scheduled_tasks.json; otherwise session-only.
