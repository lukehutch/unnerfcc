<!--
name: 'Tool Result: Account session list incomplete'
description: >-
  Note appended to the agent listing warning that account sessions could not all
  be enumerated, so those rows carry no [ref] and are not addressable by name
  yet.
ccVersion: 2.1.232
-->
(account session list incomplete just now — those rows carry no [ref] and are not messageable by name until a later listing completes)
