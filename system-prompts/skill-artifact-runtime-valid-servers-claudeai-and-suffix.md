<!--
name: 'Skill: Valid servers claude.ai and built-in suffix'
description: >-
  Suffix clarifying that built-in app servers and local MCP servers cannot be
  declared in server values.
ccVersion: 2.1.251
-->
` are valid `server` values — the Claude app's own servers (`cowork`, `workspace`, `scheduled-tasks`, `session_info` and the like) and other locally-configured MCP servers in your tool list are not.
