<!--
name: 'Tool Result: Reparsed command requires literal path'
description: Requires a literal path because the target is parsed again prior to execution.
ccVersion: 2.1.277
variables:
  - COMMAND_EXPRESSION
-->
use a literal path: the ${COMMAND_EXPRESSION} is parsed again before it runs
