<!--
name: 'Skill: Static page template runtime capability note (lead-in)'
description: >-
  Opens the static-page template's note that behavior static HTML cannot provide
  on its own is a runtime capability, ahead of the capability list.
ccVersion: 2.1.231
-->
This template builds a static page from data in the conversation. If the user wants behavior static HTML cannot provide on its own — the page reading
