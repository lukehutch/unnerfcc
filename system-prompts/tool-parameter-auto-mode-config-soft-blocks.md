<!--
name: 'Tool Parameter: Auto-mode config soft blocks'
description: >-
  Optional extra soft-block entries for the generated config — empty when
  nothing was suggested, otherwise required to lead with "$defaults".
ccVersion: 2.1.219
-->
Optional extra soft blocks. Empty when nothing was suggested. When non-empty, MUST start with "$defaults".
