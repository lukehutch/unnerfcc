<!--
name: 'Tool Result: A linked worktree path is accepted'
description: >-
  Parenthetical telling the model a path inside a linked git worktree, including
  one it creates itself, satisfies the background-isolation guard.
ccVersion: 2.1.251
-->
 (a path inside a linked git worktree, including one you create with `git worktree add`, is accepted)
