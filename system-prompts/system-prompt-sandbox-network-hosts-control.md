<!--
name: 'System Prompt: Sandbox network hosts control'
description: >-
  Explains that tool commands run in a sandbox controlling network host access
  without explicit override.
ccVersion: 2.1.270
variables:
  - TOOL_NAME
-->
By default, ${TOOL_NAME} tool commands run in a sandbox. This sandbox controls which network hosts commands may access without an explicit override.
