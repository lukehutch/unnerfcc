<!--
name: 'System Reminder: Directory sync unsettled conflicts'
description: >-
  Warns that git could not settle file conflicts cleanly and lists affected
  paths to check.
ccVersion: 2.1.251
-->
git could not settle these cleanly (a rename on both sides, a file where the other side has a directory, or a submodule), so check them before relying on them: 
