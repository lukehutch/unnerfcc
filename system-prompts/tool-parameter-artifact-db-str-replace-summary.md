<!--
name: 'Tool Parameter: Artifact DB str_replace Summary'
description: >-
  Summarizes the str_replace database action parameters for replacing exact text
  in a string field.
ccVersion: 2.1.270
-->
 'str_replace' with `collection`, `doc_id`, `field`, `old_str`, `new_str` — swaps one exact, unique piece of text inside a string field without resending the field (`replace_all`: every occurrence);
