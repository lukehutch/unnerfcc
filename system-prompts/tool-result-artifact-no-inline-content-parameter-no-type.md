<!--
name: 'Tool Result: Artifact no inline content parameter without type creation'
description: >-
  Informs the model that inline content and type-based creation are unavailable,
  directing it to build the page itself.
ccVersion: 2.1.272
variables:
  - ERROR_PREFIX
-->
${ERROR_PREFIX}. There is no inline content parameter, and creating an Artifact from a type is not available in this session, so build the page yourself.
