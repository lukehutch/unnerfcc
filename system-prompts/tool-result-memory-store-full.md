<!--
name: 'Tool Result: Shared memory store full'
description: >-
  Warns that the shared memory server is rejecting new memory files and that
  consolidating or deleting files resumes sync automatically.
ccVersion: 2.1.219
-->
The shared memory store is full — the server is rejecting new memory files (updates to already-synced files still work). Consolidate or delete memory files to free space; sync then resumes automatically.
