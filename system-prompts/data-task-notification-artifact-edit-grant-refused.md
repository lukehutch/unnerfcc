<!--
name: 'Task Notification: Artifact edit grant belongs to another user'
description: >-
  Informs that an artifact change was refused because the edit grant belongs to
  another user.
ccVersion: 2.1.257
variables:
  - REPLY_STATUS
  - FOLLOWUP_NOTE
-->
) was refused because the thread's edit grant belongs to another user, so the artifact was NOT changed, and ${REPLY_STATUS}${FOLLOWUP_NOTE}. Read the thread and make the change yourself if appropriate.
