<!--
name: 'Tool Result: Auto-replies not resumed — disarmed for the session'
description: >-
  Tells the model auto-replies are disarmed for the whole session by the user's
  kill-all-agents gesture, that a resume cannot reverse it, and not to retry.
ccVersion: 2.1.235
-->
Auto-replies were NOT resumed: they are disarmed for the whole session (the user's kill-all-agents gesture). That disarm lasts for the rest of this session and cannot be reversed by a resume — a new session re-arms on publish. Do not retry.
