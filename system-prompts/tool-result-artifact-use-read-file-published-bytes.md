<!--
name: 'Tool Result: Use read_file to get published bytes'
description: >-
  Advises taking the published bytes via the tool's read_file action before
  editing or republishing.
ccVersion: 2.1.263
variables:
  - TOOL_NAME
-->
, so take the published bytes the ${TOOL_NAME} tool's read_file action saves before editing or republishing it
