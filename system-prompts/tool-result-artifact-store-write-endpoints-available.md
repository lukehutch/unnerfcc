<!--
name: 'Tool Result: Artifact store write endpoints available'
description: >-
  Explains that no direct store-write call is served but the type declares
  endpoints.
ccVersion: 2.1.272
variables:
  - RUN_SCRIPT_INSTRUCTION
-->
no store-write call is served here, but the type declares endpoints, so ${RUN_SCRIPT_INSTRUCTION}
