<!--
name: 'Tool Result: Action read requires url'
description: Tells the model that action read requires the artifact's claude.ai URL.
ccVersion: 2.1.251
-->
action "read" requires `url` — the artifact's claude.ai URL (find it with action: "list").
