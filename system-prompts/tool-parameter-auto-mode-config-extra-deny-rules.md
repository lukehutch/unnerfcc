<!--
name: 'Tool Parameter: Auto-mode extra hard blocks'
description: >-
  Field description for optional extra deny rules — almost always empty,
  proposed only for clear-cut destructive footguns, and required to start with
  "$defaults".
ccVersion: 2.1.219
-->
Optional extra hard blocks. Almost always empty — only propose one when the recon shows a clear-cut destructive footgun. When non-empty, MUST start with "$defaults".
