<!--
name: 'Tool Result: Binary content saved for the caller'
description: >-
  Tells the web-reading agent that binary content was saved to a local file the
  harness hands the caller directly, so it should say the file was saved and
  keep every file path out of its report.
ccVersion: 2.1.232
-->
) was saved to a local file for the caller. You cannot open files here, and the harness gives the caller the path itself — say that the file was saved, but do not put any file path in your report.]
