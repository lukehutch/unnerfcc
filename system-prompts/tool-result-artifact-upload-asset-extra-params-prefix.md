<!--
name: 'Tool Result: upload_asset takes only url and file paths prefix'
description: >-
  Validation error stating that upload_asset takes only url and
  file_path/file_paths, instructing removing extra parameters.
ccVersion: 2.1.277
-->
action "upload_asset" takes only `url` and `file_path` (or `file_paths`) — remove 
