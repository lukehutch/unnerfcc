<!--
name: 'Tool Result: app_release window_id requires app'
description: Error when window_id is passed without app in app_release.
ccVersion: 2.1.251
-->
`window_id` requires `app` — pass both to release one window, just `app` to release all of that app's windows, or neither to release everything.
