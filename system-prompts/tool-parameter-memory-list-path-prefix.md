<!--
name: 'Tool Parameter: Memory list path prefix'
description: >-
  path_prefix field of the memory list tool — an optional directory-aligned
  prefix that narrows the listing to documents beneath it.
ccVersion: 2.1.251
-->
Optional directory prefix to list only documents under it (e.g. /feedback/). Matching is directory-aligned (/x is the same as /x/). Omit to list the whole store.
