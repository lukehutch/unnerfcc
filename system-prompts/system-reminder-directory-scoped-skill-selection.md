<!--
name: 'System Reminder: Use the directory-scoped skill'
description: >-
  Tells the model to prefer the directory-scoped skill when the files being
  changed live under that directory.
ccVersion: 2.1.219
variables:
  - SKILL_ROOT_DIRECTORY
-->
" skill when the files being changed are under ${SKILL_ROOT_DIRECTORY}/
