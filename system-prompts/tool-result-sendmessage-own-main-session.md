<!--
name: 'Tool Result: SendMessage own main session'
description: >-
  Explains that the target is the process's own main session and what action to
  take instead.
ccVersion: 2.1.251
variables:
  - TARGET_NAME
  - ALIAS_NOTE
  - ALTERNATIVE_ACTION
-->
'${TARGET_NAME}' is this process's own main session${ALIAS_NOTE} — from inside it, ${ALTERNATIVE_ACTION} instead.
