<!--
name: 'Tool Parameter: Artifact out_dir'
description: >-
  Describes the Artifact tool's out_dir parameter — the directory read_asset
  saves into, defaulting to the working directory, with the file named by the
  asset id plus the extension for its type.
ccVersion: 2.1.235
-->
read_asset only: directory to save the file into (default: the working directory). The file is named by the asset id plus the extension for its type.
