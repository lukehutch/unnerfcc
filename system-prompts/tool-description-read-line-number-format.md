<!--
name: 'Tool Description: Read line-number output format'
description: >-
  Read tool note describing the output format — line number, a single tab or
  colon separator, then the verbatim file content including leading whitespace.
ccVersion: 2.1.219
variables:
  - READ_OUTPUT_FORMAT_INTRO
-->
${READ_OUTPUT_FORMAT_INTRO}. Each line is the line number, a single separator (a tab or `:`), then the verbatim file content (including any leading whitespace).
