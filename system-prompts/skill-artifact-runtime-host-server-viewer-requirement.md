<!--
name: 'Skill: Host server viewer requirement note'
description: >-
  Notes that host servers only function if the viewer's Claude app has the same
  local server connected.
ccVersion: 2.1.251
-->
 A host server only answers when the viewer opens the page in a Claude app that has that same local server connected — say so to the user when you publish.
