<!--
name: 'Tool Result: App coordinates full resolution frame note'
description: >-
  Clarifies that coordinates use the full-resolution frame rather than scaled
  screenshot pixels.
ccVersion: 2.1.270
variables:
  - FRAME_WIDTH
  - FRAME_HEIGHT
-->
coordinates are in the ${FRAME_WIDTH}x${FRAME_HEIGHT} coordinate frame, not the scaled screenshot image's own pixels
