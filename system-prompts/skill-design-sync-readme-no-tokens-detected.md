<!--
name: 'Skill: Design sync README no tokens detected'
description: >-
  Documentation note explaining that no static CSS tokens were detected because
  styles are computed at runtime.
ccVersion: 2.1.251
-->
None detected — this DS may compute styles at runtime (CSS-in-JS).
