<!--
name: 'Tool Result: Parameter conflict with type_url'
description: Informs the model that the specified parameter cannot accompany type_url.
ccVersion: 2.1.251
-->
 can't accompany `type_url` — a publish with `type_url` always creates a new Artifact whose page and settings come from the type; remove 
