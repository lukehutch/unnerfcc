<!--
name: 'Tool Result: Call approval unverified on machine'
description: >-
  Tool result stating call approval could not be verified as the user's answer
  on the remote machine.
ccVersion: 2.1.251
variables:
  - MACHINE_NAME
-->
The approval for this call could not be verified as the user's own answer, so the call was not cleared to run on ${MACHINE_NAME} and nothing ran. Ask again and have the user approve from the terminal or desktop prompt, unedited.
