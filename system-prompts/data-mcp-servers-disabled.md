<!--
name: 'Data: Disabled MCP servers'
description: >-
  Lists the MCP servers that are disabled and tells the model to ask the user to
  enable them via /mcp.
ccVersion: 2.1.219
-->
Disabled (ask the user to enable via /mcp): 
