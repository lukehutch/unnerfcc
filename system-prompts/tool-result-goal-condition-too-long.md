<!--
name: 'Tool Result: Goal condition too long'
description: >-
  Tells the model the goal condition exceeds the display limit once
  canonicalized and must be shortened so the user can read all of it in the
  approval dialog.
ccVersion: 2.1.231
variables:
  - MAX_CONDITION_CHARS
-->
The goal condition exceeds ${MAX_CONDITION_CHARS} characters once canonicalized for display (tabs expand to spaces). Shorten the condition — the user must be able to read all of it in the approval dialog.
