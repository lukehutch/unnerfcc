<!--
name: 'Tool Parameter: Artifact type_url title guidance'
description: >-
  Clarifies how the title parameter is applied when instantiating an Artifact
  from a type_url.
ccVersion: 2.1.263
-->
 On a `type_url` create there is no HTML file and none of that applies: `title` is simply the new Artifact's name — what the user called it, or a short descriptive name; left out, it is named after the type (e.g. just "Slides").
