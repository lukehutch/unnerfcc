<!--
name: 'Tool Parameter: Artifact render path'
description: >-
  inputSchema description for the artifact file path to render, noting it is
  required to publish and that its basename is the last-resort title.
ccVersion: 2.1.219
-->
Path to an .html or .md file to render. Required to publish (the default action). Use a short, distinctive basename — it is the last-resort title when the HTML has no <title> and no `title` parameter is given.
