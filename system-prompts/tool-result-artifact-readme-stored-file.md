<!--
name: 'Tool Result: Artifact README is a stored file'
description: >-
  Advises that the README is a stored file rather than an inline document and
  should be read via its own call.
ccVersion: 2.1.272
-->
its README.md is a stored file rather than an inline document — read it from the system with its own call
