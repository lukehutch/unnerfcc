<!--
name: 'Tool Description: RemoteTrigger'
description: >-
  RemoteTrigger tool description (manage scheduled remote Claude Code
  agents/routines via the CCR API and inspect their recent runs and run logs).
ccVersion: 2.1.231
-->
Manage scheduled remote Claude Code agents (routines) via the claude.ai CCR API, and inspect their recent runs and run logs. Auth is handled in-process — the token never reaches the shell.
