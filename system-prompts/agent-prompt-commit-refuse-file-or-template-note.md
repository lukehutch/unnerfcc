<!--
name: 'Agent Prompt: Commit refuse file or template note'
description: 'Notes that -F, --file, and -t flags are refused during commit skill execution.'
ccVersion: 2.1.273
-->
, never from a file or template (`-F`, `--file` and `-t` are refused while this skill runs):
