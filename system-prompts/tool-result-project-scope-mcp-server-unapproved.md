<!--
name: 'Tool Result: Project-scope MCP server unapproved'
description: >-
  Explains that a project-scoped MCP server must be approved via /mcp before
  authentication or connection.
ccVersion: 2.1.257
-->
" is a project-scope MCP server (.mcp.json) that is not approved for this project — approve it via /mcp first, then authenticate or reconnect it
