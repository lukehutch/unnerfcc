<!--
name: 'System Reminder: File has no trailing newline'
description: >-
  Notes that a file lacks a trailing newline, explaining wc -l output
  discrepancy.
ccVersion: 2.1.251
-->
; the last has no trailing newline, so `wc -l` reports 
