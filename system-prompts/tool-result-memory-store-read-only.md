<!--
name: 'Tool Result: Memory store is read-only'
description: >-
  Tells the model the addressed memory store is read-only this session so
  changes will not persist, and names any writable stores.
ccVersion: 2.1.231
variables:
  - WRITABLE_STORES_NOTE
-->
 is read-only in this session; changes will not persist.${WRITABLE_STORES_NOTE}
