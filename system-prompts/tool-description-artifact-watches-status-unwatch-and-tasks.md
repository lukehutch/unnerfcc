<!--
name: 'Tool Description: Watch Status, Unwatch, and /tasks Visibility'
description: >-
  Explains status and unwatch actions and notes that session-local watches are
  visible in /tasks.
ccVersion: 2.1.251
-->
 `action: "status"` lists this session's watches (pass `url` to check one); `action: "unwatch"` with `url` stops one. Watches are session-local, and the user can see and stop them in /tasks. 
