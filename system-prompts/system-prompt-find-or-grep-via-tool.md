<!--
name: 'System Prompt: Run find or grep via the named tool'
description: Tells the model to run `find` or `grep` through the named tool.
ccVersion: 2.1.219
variables:
  - BASH_TOOL_NAME
-->
`find` or `grep` via the ${BASH_TOOL_NAME} tool
