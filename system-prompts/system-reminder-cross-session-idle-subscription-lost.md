<!--
name: 'System Reminder: Cross-session idle subscription not held'
description: >-
  Notification that a peer session is not holding an idle subscription and no
  notice will arrive.
ccVersion: 2.1.251
variables:
  - REASON
  - NEXT_ACTION
-->
" is not holding your idle subscription (${REASON}), so no idle notice will arrive from it. Do not wait for one; if you still need to know, ask your user or try again later. ${NEXT_ACTION}
