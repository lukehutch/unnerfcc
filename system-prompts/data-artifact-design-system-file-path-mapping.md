<!--
name: 'Data: Artifact design system file path mapping'
description: >-
  Maps design system specification files to project/ paths and distinguishes
  them from uploaded assets.
ccVersion: 2.1.272
-->
each file the instructions' table names is `project/<its path>` (`project/README.md`, `project/tokens.json`, `project/components/<Name>/README.md`, and so on); icons and images remain uploaded assets that the index records
