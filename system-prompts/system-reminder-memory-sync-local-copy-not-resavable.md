<!--
name: 'System Reminder: Local memory copy cannot be re-saved'
description: >-
  Tells the model the local copy was kept but cannot sync back to shared memory
  — over the sync size limit or a read-only store — so its contents must be
  saved elsewhere or trimmed to persist.
ccVersion: 2.1.251
-->
Your local copy was kept, but it cannot be re-saved to shared memory automatically (over the sync size limit or the store is read-only) — save its contents elsewhere or trim it if you want it persisted. 
