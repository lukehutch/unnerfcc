<!--
name: 'Tool Description: Memory write'
description: >-
  Model-facing description of the memory write tool — create or overwrite a
  document at a path in the named store, with content replacing the whole
  document.
ccVersion: 2.1.251
-->
Save a memory document to a memory store (named by its store id), creating or overwriting the document at the given path. `content` replaces the entire document — include every line you intend to keep.
