<!--
name: 'System Reminder: Large file — grep for targeted searches'
description: >-
  Large-file reading guidance telling the model to grep the file directly when
  it only needs to find a line or locate a string.
ccVersion: 2.1.219
-->
- For targeted searches (find a line, locate a string): use grep on the file directly.
