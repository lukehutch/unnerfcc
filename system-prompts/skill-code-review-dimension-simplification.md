<!--
name: 'Skill: Code Review (simplification dimension)'
description: >-
  Review dimension asking the model to flag unnecessary complexity the diff adds
  and name the simpler form that does the same job.
ccVersion: 2.1.219
-->
### Simplification

Flag unnecessary complexity the diff adds: redundant or derivable state,
copy-paste with slight variation, deep nesting, dead code left behind. Name
the simpler form that does the same job.
