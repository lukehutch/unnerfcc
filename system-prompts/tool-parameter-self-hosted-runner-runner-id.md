<!--
name: 'Tool Parameter: Self-hosted runner runner id'
description: >-
  Describes the tagged runner id input, that the server rejects it with Conflict
  when it no longer matches the session assignment, and that any value is
  accepted for a stuck session whose runner is gone.
ccVersion: 2.1.231
-->
Tagged runner id (ccrunner_…) the caller observed failing. Server verifies this still matches the session assignment; mismatch returns Conflict. For a stuck session whose runner is gone, any value is accepted.
