<!--
name: 'System Reminder: Attached images file paths'
description: >-
  Provides the disk locations of multiple attached images while instructing not
  to re-read them solely for viewing.
ccVersion: 2.1.251
variables:
  - SAVED_IMAGE_PATHS_LIST
-->
 attached images, in display order, are also saved at ${SAVED_IMAGE_PATHS_LIST}. Use these file paths only if a task needs the image files themselves (for example, copying them into a file you are creating) — the images are already visible to you, so do not read the files just to view them.
