<!--
name: 'System Reminder: Batch related operations into one call'
description: >-
  Tells the model batching related operations into a single call is faster than
  issuing them separately or in parallel, and that this result needs no action.
ccVersion: 2.1.251
-->
: [{...}, {...}]`). Batching related operations into a single call is faster than issuing them as separate or parallel calls. No action needed for this result.</system-reminder>
