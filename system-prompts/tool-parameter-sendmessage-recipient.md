<!--
name: 'Tool Parameter: SendMessage recipient'
description: >-
  Recipient of the message — a peer session name from the agent listing, or an
  explicit uds:<socket> or bridge:<session id> address.
ccVersion: 2.1.219
variables:
  - LIST_AGENTS_TOOL_NAME
-->
Recipient: a peer session name from ${LIST_AGENTS_TOOL_NAME}, or an explicit uds:<socket> / bridge:<session id> address
