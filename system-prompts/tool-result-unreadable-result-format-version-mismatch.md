<!--
name: 'Tool Result: Unreadable result format version mismatch'
description: >-
  Tool result reporting a version mismatch where the remote machine answered in
  an unreadable format.
ccVersion: 2.1.251
variables:
  - MACHINE_NAME
-->
${MACHINE_NAME} answered in a result format this session cannot read (Claude Code version mismatch?); the command probably ran there — check its effect before repeating it.
