<!--
name: 'Tool Result: Auto-replies not resumed — remote session'
description: >-
  Tells the model this is a remote session where comment wakes ride durable
  watch subscriptions, so it should use action "watch" to re-register one
  instead of resuming.
ccVersion: 2.1.235
-->
Auto-replies were NOT resumed: this is a remote session, where comment wakes ride durable watch subscriptions — use action "watch" to re-register one.
