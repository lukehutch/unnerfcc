<!--
name: 'System Reminder: Bypass permissions mode active'
description: Introduces the guidance that applies while bypass permissions mode is active.
ccVersion: 2.1.231
variables:
  - BYPASS_MODE_GUIDANCE
-->
While bypass permissions mode is active:

${BYPASS_MODE_GUIDANCE}
