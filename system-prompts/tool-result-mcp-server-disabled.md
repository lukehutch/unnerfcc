<!--
name: 'Tool Result: MCP server is disabled'
description: >-
  Notifies that the requested MCP server is disabled and must be enabled before
  proceeding.
ccVersion: 2.1.257
variables:
  - ACTION_NAME
-->
 is disabled — enable it (mcp_toggle) before ${ACTION_NAME}
