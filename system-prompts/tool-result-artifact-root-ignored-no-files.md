<!--
name: 'Tool Result: Root ignored when no files sent'
description: >-
  Informs that root was ignored because no files were sent and the page was read
  directly from file_path.
ccVersion: 2.1.277
-->
`root` was ignored: it only resolves `files` sources, and this call sent no `files` — the page was read from `file_path` as given (
