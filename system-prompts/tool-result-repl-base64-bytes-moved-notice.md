<!--
name: 'Tool Result: REPL base64 image bytes moved notice'
description: >-
  Notes the number of base64 bytes moved to the REPL result subject to the
  per-result cap.
ccVersion: 2.1.251
variables:
  - BYTE_COUNT
-->
 base64 chars — ${BYTE_COUNT} bytes moved to the REPL result, subject to its per-result ${BYTE_COUNT} cap]
