<!--
name: 'Skill: Code review angle — reuse (body)'
description: >-
  Body of the reuse cleanup angle telling the finder to flag new code that
  re-implements something the codebase already has, Grepping shared utility
  modules and adjacent files and naming the existing helper to call instead.
ccVersion: 2.1.219
-->
Flag new code that re-implements something the codebase
already has — Grep shared/utility modules and files adjacent to the change,
and name the existing helper to call instead.
