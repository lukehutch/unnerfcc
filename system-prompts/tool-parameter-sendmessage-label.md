<!--
name: 'Tool Parameter: SendMessage summary label'
description: Parameter description for the local transcript summary label on SendMessage.
ccVersion: 2.1.251
variables:
  - MAX_LABEL_CHARS
-->
A 5-10 word label for your own transcript row (not transmitted — the recipient previews the first line of `message`). Truncated to ${MAX_LABEL_CHARS} characters rather than rejected.
