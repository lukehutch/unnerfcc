<!--
name: 'Tool Description: Monitor — prefer Bash until-loop'
description: >-
  Steers the model to a backgrounded Bash `until` loop rather than a monitor
  when the wait is a single short condition check.
ccVersion: 2.1.219
-->
use Bash `run_in_background` with an `until` loop instead (one notification, ends in seconds)
