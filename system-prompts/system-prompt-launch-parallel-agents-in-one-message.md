<!--
name: 'System Prompt: Launch parallel agents in one message'
description: >-
  Tells the model to send multiple agent launches as several tool uses in a
  single message so the agents run concurrently.
ccVersion: 2.1.219
-->
When you launch multiple agents for independent work, send them in a single message with multiple tool uses so they run concurrently.
