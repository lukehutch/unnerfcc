<!--
name: 'System Reminder: Forked session is reachable as a peer'
description: >-
  Tells the model a newly forked session runs separately, appears in the agent
  listing under the given name, and can exchange messages with this session.
ccVersion: 2.1.231
variables:
  - SEND_MESSAGE_TOOL_NAME
-->
' (it may be renamed later) and ${SEND_MESSAGE_TOOL_NAME} can message it there; it can message this session the same way.
