<!--
name: 'Agent Prompt: Date parser INVALID fallback'
description: >-
  Instructs the date-parsing model to answer with exactly "INVALID" when the
  input is incomplete or cannot be confidently parsed into a date.
ccVersion: 2.1.219
-->
If the input is incomplete or you cannot confidently parse it into a valid date, respond with exactly "INVALID" (nothing else).
