<!--
name: 'Data: Artifact publish selective update rule'
description: >-
  Directs publishing only modified files and omitting the index unless index
  keys change.
ccVersion: 2.1.273
-->
) and publish only those the same way (`file_path`: one changed file's absolute path); send the index only when you 
