<!--
name: 'Tool Description: Artifact DB tool read action instructions'
description: Instructions for reading an artifact database via dedicated DB tool.
ccVersion: 2.1.257
-->
this tool reads and writes it as the user; every call takes the artifact's `url`. To read, pass `action`:
