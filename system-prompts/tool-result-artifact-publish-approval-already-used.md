<!--
name: 'Tool Result: Artifact Publish Approval Already Used'
description: >-
  Reports that the publish approval was already consumed by a prior run and
  instructs retrying as a new call.
ccVersion: 2.1.270
-->
file_path: the approval for this publish was already used by an earlier run of the same tool call, so nothing was published by this run — retry the publish as a new call
