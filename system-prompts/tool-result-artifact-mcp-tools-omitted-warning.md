<!--
name: 'Tool Result: Artifact omitted tools warning'
description: >-
  Warns that calls to ungranted local tools will fail for viewers and instructs
  telling the user which tools were omitted.
ccVersion: 2.1.272
variables:
  - TOOL_NAMES
-->
 — the page's calls to ${TOOL_NAMES} fail for every viewer; tell the user which local tools were left out
