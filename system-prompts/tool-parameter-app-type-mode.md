<!--
name: 'Tool Parameter: app_type mode'
description: mode parameter description for app_type (insert vs replace).
ccVersion: 2.1.251
-->
insert (default) writes at the caret/selection. replace selects all then writes, clearing the field in one call — use when you need to overwrite the whole field rather than append.
