<!--
name: 'Tool Result: File transfer bridge ID is stale'
description: >-
  Clause of a cross-session file-transfer failure telling the model the peer
  session may have ended or restarted, so the bridge ID it addressed is stale.
ccVersion: 2.1.231
variables:
  - LIST_AGENTS_RETRY_HINT
-->
 — the peer session may have ended or restarted, so this bridge ID is stale.${LIST_AGENTS_RETRY_HINT}
