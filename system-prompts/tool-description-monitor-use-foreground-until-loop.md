<!--
name: 'Tool Description: Use a foreground until-loop instead'
description: >-
  Tells the model to use a foreground Bash `until` loop instead of the
  alternative it would otherwise reach for.
ccVersion: 2.1.219
-->
use a foreground Bash `until` loop instead
