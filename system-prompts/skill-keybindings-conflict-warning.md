<!--
name: 'Skill: Keybinding conflict warning'
description: >-
  Requires proactively warning the user when a chosen key conflicts with
  reserved shortcuts or common tools such as tmux and screen.
ccVersion: 2.1.219
-->
3. Warn the user proactively if they choose a key that conflicts with reserved shortcuts or common tools like tmux (`ctrl+b`) and screen (`ctrl+a`)
