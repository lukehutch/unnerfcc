<!--
name: 'Tool Result: Artifact database document over the result cap'
description: >-
  Tail of an artifact database read result telling the model that a document
  larger than the result cap cannot be rendered in the tool result at all, and
  only the artifact page's own code can read it.
ccVersion: 2.1.231
-->
. A single document larger than this result cap cannot be rendered here — only the artifact page itself can read it.]
