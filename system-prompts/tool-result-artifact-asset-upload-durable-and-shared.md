<!--
name: 'Tool Result: Uploaded asset is durable and readable by every viewer'
description: >-
  Tail of the artifact asset-upload result — the relative url resolves in every
  view, the id belongs in the artifact's database when rows point at it,
  everyone who can open the artifact can load the file, and the upload lasts
  until deleted with action "delete_asset".
ccVersion: 2.1.235
-->
> — which resolves in every view of the artifact; store the id in the artifact's database if rows need to point at it. Everyone who can open the artifact can load this file; the upload is durable until deleted with action "delete_asset".
