<!--
name: 'Tool Result: Viewer loaded artifact diagnostics unread prefix'
description: >-
  Prefix reporting that diagnostics were captured but could not be read into the
  tool result.
ccVersion: 2.1.251
variables:
  - ARTIFACT_URL
  - VERSION
-->
A viewer loaded ${ARTIFACT_URL} (version ${VERSION}) and diagnostics WERE captured, but none could be read into this result (
