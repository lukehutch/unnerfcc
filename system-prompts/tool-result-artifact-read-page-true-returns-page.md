<!--
name: 'Tool Result: Artifact read page true returns page'
description: >-
  Explains that calling read with page: true on the specified tool returns the
  page itself.
ccVersion: 2.1.270
variables:
  - TOOL_NAME
-->
; the ${TOOL_NAME} tool's read with `page: true` returns the page itself
