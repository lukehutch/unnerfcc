<!--
name: 'Tool Result: Artifact read_db token collections and doc IDs'
description: >-
  Details collection and doc_id parameters for reading design system tokens via
  read_db.
ccVersion: 2.1.272
-->
, and a `collection` and `doc_id`: "api" and "tokens.md" (palette, type, fonts); "ds" and "tokens" (exact token values); "ds" and "meta" (the system's record). Where the README's index is shown, read a card it names by the collection and doc id the index gives.
