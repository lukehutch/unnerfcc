<!--
name: 'System Reminder: Large file — jq for targeted queries'
description: >-
  Large-file reading guidance telling the model to run jq on the file directly
  when it only needs one value or a filtered field.
ccVersion: 2.1.219
-->
- For targeted queries (find a value, filter by field): use jq on the file directly.
