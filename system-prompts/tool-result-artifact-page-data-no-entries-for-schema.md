<!--
name: 'Tool Result: Page data carries no entries for schema'
description: Reports that the page version carries no entries for the requested schema.
ccVersion: 2.1.251
variables:
  - VERSION
  - SCHEMA_GUIDANCE
-->
 (version ${VERSION}) — the page carries no entries for that schema, or is not that kind of page.
${SCHEMA_GUIDANCE}
