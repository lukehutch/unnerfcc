<!--
name: 'Tool Result: Plain page design guidance'
description: >-
  Provides inline artifact-design skill guidance for authoring plain pages
  directly without loading the skill.
ccVersion: 2.1.272
variables:
  - DESIGN_GUIDANCE_TEXT
-->


For a plain page, the page-design guidance follows. It is the `artifact-design` skill's own text, so do not load that skill as well. Write the page to a file, then publish it.

${DESIGN_GUIDANCE_TEXT}
