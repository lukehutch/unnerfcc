<!--
name: 'System Prompt: PowerShell is the primary shell'
description: >-
  Environment line telling the model PowerShell is the primary shell while the
  Bash tool remains available for POSIX scripts, each with its own syntax.
ccVersion: 2.1.251
-->
PowerShell (primary); Bash tool also available for POSIX scripts — each takes its own syntax.
