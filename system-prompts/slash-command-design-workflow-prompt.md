<!--
name: 'Slash Command: /design workflow prompt'
description: >-
  Fragment of the /design slash-command prompt (built in uOm and returned by
  getPromptForCommand) instructing the model to confirm the Claude Design MCP
  tools are connected and dispatch on the arguments.
ccVersion: 2.1.181
-->
`) are available to you. If they are not, tell the user to run `/design login` (or add the `
