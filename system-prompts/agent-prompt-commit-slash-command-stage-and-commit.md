<!--
name: 'Agent Prompt: Commit slash command (stage and commit)'
description: >-
  Step 2 of the commit slash-command prompt — stage the relevant files and
  always pass the commit message through a heredoc or here-string.
ccVersion: 2.1.231
-->


2. Stage the relevant files and create the commit. To ensure good formatting, ALWAYS pass the commit message via a 
