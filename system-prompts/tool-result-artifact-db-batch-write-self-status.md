<!--
name: 'Tool Result: Artifact DB Batch Write Self Status'
description: Reports the status of the specific write where a batch was interrupted.
ccVersion: 2.1.251
variables:
  - WRITES_BEFORE_STATUS
  - WRITE_INDEX
-->
). The ${WRITES_BEFORE_STATUS}; writes[${WRITE_INDEX}] itself 
