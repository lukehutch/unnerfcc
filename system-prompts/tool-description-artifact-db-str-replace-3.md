<!--
name: 'Tool Description: Artifact database str_replace operation'
description: >-
  Describes the str_replace database write operation for swapping substrings
  inside a field.
ccVersion: 2.1.272
-->
 "str_replace" swaps one exact, unique `old_str` for `new_str` inside a string `field` without resending it (`replace_all: true` for every occurrence),
