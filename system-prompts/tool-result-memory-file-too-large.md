<!--
name: 'Tool Result: Memory file exceeds the per-file size limit'
description: >-
  Tells the model the server rejected an oversized memory file and that its
  content must be split into smaller files.
ccVersion: 2.1.219
-->
The server rejected a memory file over its per-file size limit. Split the file's content into smaller files.
