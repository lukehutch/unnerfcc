<!--
name: 'Skill: /loop schedule recurring task cron'
description: >-
  Tells the model to schedule a recurring cron that runs the loop tasks each
  tick, then run the first tick immediately.
ccVersion: 2.1.219
-->
`. Schedule a recurring cron that runs those tasks each tick, then run the first tick immediately.
