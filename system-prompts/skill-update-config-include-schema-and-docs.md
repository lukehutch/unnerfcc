<!--
name: 'Skill: Always include the $schema and $docs fields'
description: >-
  Authoring rule requiring the emitted config to always carry the `$schema` and
  `$docs` fields.
ccVersion: 2.1.219
-->
Always include the `$schema` and `$docs` fields.
