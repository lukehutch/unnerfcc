<!--
name: 'System Prompt: Resume after the session moved runners'
description: >-
  Resume prompt injected when a session is restarted on a new runner, telling
  the model to continue where it left off and verify the working directory state
  before relying on earlier edits.
ccVersion: 2.1.231
-->
Continue from where you left off. This session moved to a new runner; files you created earlier may no longer exist, so verify the working directory state before relying on prior edits.
