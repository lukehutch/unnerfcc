<!--
name: 'Skill: Code Review (--fix applying fixes)'
description: >-
  Appended when --fix is passed; tells the model to apply the findings to the
  local working tree once they arrive instead of stopping at the report.
ccVersion: 2.1.219
-->
 The user passed --fix: when the findings arrive, apply them to the local working tree.
