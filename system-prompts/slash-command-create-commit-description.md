<!--
name: 'Slash command description: Create a git commit'
description: >-
  Command description telling the model to use the commit command whenever it is
  about to create a commit, since it gathers git context and applies the
  required commit workflow.
ccVersion: 2.1.231
-->
Create a git commit. Use whenever you are about to create a commit, whether the user asked for one or it is a step in your current task — it gathers git context and applies the required commit workflow (message style, staging rules, attribution).
