<!--
name: 'Tool Result: foreignObject not allowed in SVG'
description: >-
  Publish-time rejection explaining foreignObject embeds arbitrary HTML inside
  SVG, and requiring native SVG shapes and <text> instead.
ccVersion: 2.1.219
-->
foreignObject embeds arbitrary HTML inside SVG — draw with native SVG shapes and <text> instead.
