<!--
name: 'Tool Description: Fork runs in the background'
description: >-
  Agent tool note that a fork runs in the background with its tool output kept
  out of the parent's context, and that a fork must execute directly rather than
  re-delegate.
ccVersion: 2.1.219
-->


A fork runs in the background and keeps its tool output out of your context. If you are the fork, execute directly — don't re-delegate.
