<!--
name: 'Tool Parameter: resume_replies unavailable in a remote session'
description: >-
  Remote-session branch of the artifact action field, telling the model
  resume_replies is unsupported there because automatic replies run only in a
  live session, so it should say so rather than call it.
ccVersion: 2.1.235
-->
 'resume_replies' (re-enabling automatic comment replies the user stopped) isn't supported from this remote session either — automatic replies run only in a live session, so say so rather than calling it.
