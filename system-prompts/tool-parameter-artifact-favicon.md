<!--
name: 'Tool Parameter: Artifact favicon'
description: >-
  Model-facing input_schema param description for the Artifact tool's favicon
  (browser-tab emoji) field, serialized into the model's tool list
ccVersion: 2.1.191
-->
Browser-tab icon: one or two emoji (e.g. "📊"). No markup. Keep stable across redeploys; change only on a hard topic pivot.
