<!--
name: 'Skill: Describe connector by its tools'
description: >-
  Tells the model to describe connectors by their tool names and use the
  resolved display name as server.
ccVersion: 2.1.251
-->
 by the tools it provides (its `mcp__<id>__…` tool names), since the user cannot see the id — and use that name as `server` and in the page's calls; the id itself is refused at publish because no viewer can resolve it.
