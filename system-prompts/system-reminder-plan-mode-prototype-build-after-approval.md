<!--
name: 'System Reminder: Plan mode prototype build after approval'
description: >-
  Tail of plan mode's prototype-offer block telling the model to invoke the
  prototype skill to build and publish the prototype once the user approves the
  plan and plan mode has ended.
ccVersion: 2.1.222
-->
, and once the user approves and plan mode has ended, invoke the prototype skill to build and publish it.
