<!--
name: 'Tool Result: Artifact publish missing required parameters'
description: >-
  Informs the model that file_path and favicon are required to publish an
  artifact and instructs writing an HTML file first.
ccVersion: 2.1.272
-->
file_path and favicon required to publish. Write the page to an .html file, then call again with its path as `file_path` and a one-or-two-emoji `favicon`
