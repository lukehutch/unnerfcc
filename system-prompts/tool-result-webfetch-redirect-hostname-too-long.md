<!--
name: 'Tool Result: Redirect hostname is not a fetchable address'
description: >-
  Marks a redirect target whose hostname is longer than any DNS name, so it
  cannot be fetched.
ccVersion: 2.1.232
variables:
  - MAX_HOSTNAME_LENGTH
-->
 [hostname longer than any DNS name (${MAX_HOSTNAME_LENGTH} characters): not a fetchable address]
