<!--
name: 'System Reminder: Directory sync nested repo sync blocked lead-in'
description: Explains that directory sync never enters nested git repositories.
ccVersion: 2.1.251
-->
 inside the project (nested); sync never enters a nested repository, so NOTHING inside 
