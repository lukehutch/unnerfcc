<!--
name: 'Tool Result: Ask user what data page expects'
description: >-
  Guidance to ask the user what data the page expects if it cannot be inferred
  from file names.
ccVersion: 2.1.251
-->
; after creating, ask the user what data the page expects if it is not obvious from the file names
