<!--
name: 'System Reminder: Use Read tool instead of cat for artifact files'
description: >-
  Reminds the model to open files with the Read tool rather than cat to avoid
  Bash inline output truncation.
ccVersion: 2.1.272
-->
Open these files with the Read tool rather than `cat`: a file past the Bash tool's inline output limit does not come back in full.
