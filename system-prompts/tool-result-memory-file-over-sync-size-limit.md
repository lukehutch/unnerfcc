<!--
name: 'Tool Result: Memory file exceeds the sync size limit'
description: >-
  Post-write warning that an oversized memory file stayed local and will not
  sync to shared memory, and that it must be split into smaller files under the
  limit.
ccVersion: 2.1.219
-->
 sync limit — it was saved locally but will NOT be synced to shared/server memory. Split it into smaller files under the limit.
