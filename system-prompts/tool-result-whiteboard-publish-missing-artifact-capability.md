<!--
name: 'Tool Result: Whiteboard publish lacks the artifact capability'
description: >-
  Tells the model the whiteboard's Send to Claude button works only through the
  `artifact` capability, which this publish does not declare, so shipping it
  would put up a board that can never send back.
ccVersion: 2.1.251
-->
This page is a whiteboard, and its send-back button works only through the `artifact` capability (artifact publish; legacy spelling `self`), which this publish does not declare — shipping it would put up a board that can never send back. 
