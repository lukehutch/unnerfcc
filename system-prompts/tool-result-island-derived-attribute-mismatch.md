<!--
name: 'Tool Result: Island derives a different attribute value'
description: >-
  Publish-time mismatch telling the model the visible text and the island's
  derived attribute disagree and must be changed together, since the attribute
  is the wire contract.
ccVersion: 2.1.219
variables:
  - DERIVED_VALUE
-->
" but the island derives "${DERIVED_VALUE}" — flip them together (text stays yours; the attribute is the wire contract).
