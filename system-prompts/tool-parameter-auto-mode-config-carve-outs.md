<!--
name: 'Tool Parameter: Auto-mode config carve-outs'
description: >-
  Optional carve-out entries for the generated permission config — empty when
  nothing was suggested, otherwise required to lead with the literal "$defaults"
  entry.
ccVersion: 2.1.219
-->
Optional carve-outs. Empty when nothing was suggested. When non-empty, MUST start with the literal entry "$defaults".
