<!--
name: 'Tool Result: PDF page count exceeded range guidance'
description: >-
  Informs the model that the PDF has too many pages to read at once and directs
  using the pages parameter.
ccVersion: 2.1.272
variables:
  - MAX_PAGES
-->
 pages, which is too many to read at once. Use the pages parameter to read specific page ranges (e.g., pages: "1-5"). Maximum ${MAX_PAGES} pages per request.
