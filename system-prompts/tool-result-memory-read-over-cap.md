<!--
name: 'Tool Result: Memory document over the read cap'
description: >-
  Tells the model the memory document exceeds the read cap so its content is not
  returned.
ccVersion: 2.1.231
variables:
  - READ_CAP_BYTES
-->
 bytes, over the ${READ_CAP_BYTES}-byte read cap, so its content is not returned.
