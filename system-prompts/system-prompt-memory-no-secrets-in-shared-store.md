<!--
name: 'System Prompt: Never write secrets into a shared memory'
description: >-
  Tells the model never to put secrets or credentials in a memory because
  project stores are shared with every collaborator and such writes are refused.
ccVersion: 2.1.251
-->
Never write secrets or credentials into a memory — project stores are shared with every collaborator, and such writes are refused in every store.
