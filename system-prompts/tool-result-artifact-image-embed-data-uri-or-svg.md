<!--
name: 'Tool Result: Embed Image as Data URI or SVG'
description: >-
  Suggests embedding the image as a data URI or inline SVG and publishing the
  HTML page.
ccVersion: 2.1.251
-->
embed it as a data: URI or inline SVG — and publish that page instead.
