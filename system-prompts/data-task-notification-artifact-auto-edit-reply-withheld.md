<!--
name: 'Task Notification: Artifact auto-edit reply withheld'
description: >-
  Task notification telling the model the artifact was auto-edited but the
  summary reply was withheld because another session's reply already stands in
  the thread, so the change and that reply need review.
ccVersion: 2.1.231
-->
, but the summary reply was withheld: another session's reply already stands in the thread. The artifact WAS changed — review the change and the thread's existing reply.
