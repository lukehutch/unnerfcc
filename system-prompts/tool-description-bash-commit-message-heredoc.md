<!--
name: 'Tool Description: Bash commit message heredoc'
description: >-
  Commit-message example in the Bash tool docs showing the `git commit -m "$(cat
  <<'EOF' ...)"` heredoc form.
ccVersion: 2.1.219
-->
```
git commit -m "$(cat <<'EOF'
Commit message here.
