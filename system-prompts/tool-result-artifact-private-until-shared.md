<!--
name: 'Tool Result: Artifacts stay private until shared'
description: >-
  Tail of the artifact publish result telling the model the page stays private
  until the user shares it from the page's share menu, and where the user can
  browse their artifacts.
ccVersion: 2.1.232
variables:
  - ARTIFACTS_GALLERY_URL
-->
Artifacts are private unless shared from the page's share menu; with Claude Code on the web, the user can browse theirs at ${ARTIFACTS_GALLERY_URL}.
