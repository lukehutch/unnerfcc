<!--
name: 'Data: Artifact start files mode publish parameters'
description: >-
  Specifies publication parameters when initializing an artifact on files and
  notes transition from store.
ccVersion: 2.1.273
-->
, `root`: that folder, `file_path`: the index's absolute path, `files`: the rest by their `project/…` paths), and tell the user whether its earlier content was carried over; from then on its page shows only those files, nothing from its old store
