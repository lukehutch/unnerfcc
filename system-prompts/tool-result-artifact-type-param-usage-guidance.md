<!--
name: 'Tool Result: Type parameter usage guidance'
description: >-
  Clarifies that `type` is for listing type instances and `type_url` is for
  creating new artifacts from a type.
ccVersion: 2.1.257
-->
`type` applies only to action "list" (it names the type whose Artifacts to list) — to create from a type, pass its link as `type_url`
