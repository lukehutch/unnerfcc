<!--
name: 'Tool Parameter: Artifact force true discards newer version'
description: >-
  Warns that force:true discards a newer published version of an artifact and
  should only be used when explicitly requested.
ccVersion: 2.1.251
-->
 force:true discards that newer version — someone's save from the page itself, or another session's publish — so use it only when the user explicitly asks to overwrite.
