<!--
name: 'Tool Result: Artifact run_script mutation available'
description: Notes that the tool can mutate the store via run_script in mutation mode.
ccVersion: 2.1.272
variables:
  - ARTIFACT_TOOL_NAME
-->
the ${ARTIFACT_TOOL_NAME} tool's `action: "run_script"` with `mode: "mutation"` can write it (`get_endpoints` first)
