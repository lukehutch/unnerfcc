<!--
name: 'Tool Description: WebFetch artifact links exception'
description: >-
  Explains that claude.ai artifact links can be fetched with WebFetch using the
  claude.ai login rather than curl.
ccVersion: 2.1.270
-->
 Exception: claude.ai artifact links (claude.ai/artifact/{id} or claude.ai/code/artifact/{uuid}) ARE fetchable via your claude.ai login — use WebFetch, not curl (curl gets the SPA shell or a Cloudflare 403).
