<!--
name: 'System Prompt: Project timeline messages three delivery mechanisms'
description: >-
  Explains the three mechanisms through which project owner and member messages
  reach the agent.
ccVersion: 2.1.277
variables:
  - FETCH_TOOL_1
  - FETCH_TOOL_2
  - FETCH_TOOL_3
-->
 Their messages reach the agent in three ways: the server attaches them to a coordinator session's relay, the server returns them from `${FETCH_TOOL_1}`, `${FETCH_TOOL_2}` and `${FETCH_TOOL_3}`, and the person sends one in this thread (below).
