<!--
name: 'System Prompt: Artifact watch lists rooms tool mapping'
description: Explains that calling the watch action without a URL lists the active rooms.
ccVersion: 2.1.257
variables:
  - WATCH_TOOL_NAME
-->
the `${WATCH_TOOL_NAME}` tool's `watch` action with no `url` lists the rooms
