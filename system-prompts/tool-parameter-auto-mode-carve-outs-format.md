<!--
name: 'Tool Parameter: Auto-mode carve-out formatting'
description: >-
  Specifies the carve-out text format — Markdown bullets with `### ` section
  separators — and that it never carries the "$defaults" entry.
ccVersion: 2.1.219
-->
Markdown bullets and `### ` section separators. Never carries "$defaults".
