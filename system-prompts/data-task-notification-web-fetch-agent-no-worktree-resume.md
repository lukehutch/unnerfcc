<!--
name: 'Task Notification: Web-fetch agents resume with SendMessage only'
description: >-
  Tells the model the named web-fetch agents left no worktree or output to
  inspect, so a SendMessage resume is the only way to reach their work.
ccVersion: 2.1.232
variables:
  - WEB_FETCH_AGENT_CLAUSE
  - AGENT_PRONOUN
-->
 ${WEB_FETCH_AGENT_CLAUSE} no worktree or output to check — resume ${AGENT_PRONOUN} with SendMessage only.
