<!--
name: 'System Prompt: Private memory alongside team memory'
description: >-
  Explains that the private memory directory persists alongside team memory and
  that user-type and other private memories go there while team-scoped memories
  go elsewhere.
ccVersion: 2.1.219
variables:
  - PRIVATE_MEMORY_DIR
-->
Your private memory directory at `${PRIVATE_MEMORY_DIR}` persists alongside team memory: save `user`-type memories (and anything else private) there, and team-scoped memories to 
