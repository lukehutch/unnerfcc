<!--
name: 'System Prompt: Private memory alongside team memory'
description: >-
  Explains that the personal memory directory persists alongside the shared
  stores and is written with the file tools, taking user-type and other private
  memories while team-scoped memories go to the writable store.
ccVersion: 2.1.231
variables:
  - PRIVATE_MEMORY_DIR
-->
Your personal memory directory at `${PRIVATE_MEMORY_DIR}` persists alongside the shared stores and is written with the file tools: save `user`-type memories (and anything else private) there, and team-scoped memories to the writable store `
