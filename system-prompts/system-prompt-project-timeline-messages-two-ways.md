<!--
name: 'System Prompt: Project timeline messages two delivery mechanisms'
description: >-
  Explains the two mechanisms through which project owner and member messages
  reach the agent.
ccVersion: 2.1.277
variables:
  - FETCH_TOOL_1
  - FETCH_TOOL_2
  - FETCH_TOOL_3
-->
 Their messages reach the agent in two ways: the server attaches them to a coordinator session's relay, and the server returns them from `${FETCH_TOOL_1}`, `${FETCH_TOOL_2}` and `${FETCH_TOOL_3}`.
