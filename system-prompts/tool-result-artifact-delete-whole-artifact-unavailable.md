<!--
name: 'Tool Result: Deleting whole artifact unavailable'
description: >-
  Explains that deleting an entire artifact is unavailable and delete only
  removes individual assets by ID.
ccVersion: 2.1.257
-->
deleting a whole Artifact is not available in this session — `delete` here takes `path` (an uploaded asset's id, from a `scope: "assets"` listing) and removes that one asset
