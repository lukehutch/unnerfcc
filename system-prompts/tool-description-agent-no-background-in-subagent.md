<!--
name: 'Tool Description: Agent — no background subagents here'
description: >-
  Notes that `run_in_background` and `name` are unavailable in this context, so
  only synchronous subagents can be spawned.
ccVersion: 2.1.219
-->

- `run_in_background` and `name` are unavailable here — only synchronous subagents.
