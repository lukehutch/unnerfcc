<!--
name: 'Tool Result: System-level shortcut needs a grant'
description: >-
  Tells the model the requested key combination is a system-level shortcut and
  needs the `systemKeyCombos` grant requested via request_access.
ccVersion: 2.1.219
variables:
  - KEY_COMBINATION
-->
"${KEY_COMBINATION}" is a system-level shortcut. Request the `systemKeyCombos` grant via request_access to use it.
