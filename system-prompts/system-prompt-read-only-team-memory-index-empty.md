<!--
name: 'System Prompt: Read-only team memory index (empty)'
description: >-
  Tells the model it has a read-only team memory index at the given path which
  is currently empty.
ccVersion: 2.1.219
variables:
  - TEAM_MEMORY_INDEX_PATH
-->
You have a read-only team memory index at `${TEAM_MEMORY_INDEX_PATH}` (currently empty).
