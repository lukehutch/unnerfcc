<!--
name: 'Tool Result: Text files require separate asset upload'
description: >-
  Explains that text files require separate approval and must be uploaded
  individually using file_path.
ccVersion: 2.1.277
-->
a CSV, Markdown, JSON or plain-text file is approved on its own — upload it in its own call with `file_path`; the other files can go together.
