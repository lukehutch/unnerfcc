<!--
name: 'Tool Result: files parameter requires file_path'
description: >-
  Informs the model that publishing multiple files requires specifying one
  primary file as file_path.
ccVersion: 2.1.251
-->
`files` needs `file_path` — name one data file as `file_path` and the rest in `files`
