<!--
name: 'Tool Description: Bash foreground sleep blocked'
description: >-
  Bash-description note that foreground `sleep` is blocked and the model should
  use Monitor with an until-loop to wait on a condition.
ccVersion: 2.1.219
-->
 Foreground `sleep` is blocked; use Monitor with an until-loop to wait on a condition.
