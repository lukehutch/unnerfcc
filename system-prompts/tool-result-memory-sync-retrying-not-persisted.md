<!--
name: 'Tool Result: Memory sync retrying, not yet persisted'
description: >-
  Tells the model a memory write was saved locally while sync is still retrying,
  so it must be treated as not yet persisted to shared memory.
ccVersion: 2.1.219
-->
) and is retrying — recovery is not yet confirmed. This write was saved locally; treat it as NOT yet persisted to shared memory until sync succeeds.
