<!--
name: 'Tool Parameter: Artifact read render'
description: Specifies returning the rendered page content when reading an artifact.
ccVersion: 2.1.270
-->
read only: true returns the rendered page in cases where a read otherwise returns something else.
