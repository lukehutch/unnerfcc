<!--
name: 'Tool Result: Worker process access prefix'
description: >-
  Explains that an unlisted application may be a worker process spawned by an
  already-granted launcher.
ccVersion: 2.1.270
-->
 in the installed-apps list — likely a worker process spawned by a launcher you already granted (e.g. LibreOffice's simpress.exe launches soffice.bin, which owns the actual window). Pass the exact 
