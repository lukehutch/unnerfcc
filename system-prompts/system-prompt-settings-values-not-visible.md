<!--
name: 'System Prompt: Settings values not visible'
description: >-
  Notes the model cannot see the configured values and that the user can inspect
  them with `claude config list` or in ~/.claude/settings.json.
ccVersion: 2.1.219
-->
. To see values, the user can run `claude config list` or open `~/.claude/settings.json`.
