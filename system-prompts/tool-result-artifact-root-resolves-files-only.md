<!--
name: 'Tool Result: Root resolves files sources only'
description: >-
  Explains that root only resolves files sources when file_path is not under
  root.
ccVersion: 2.1.277
-->
) only resolves `files` sources and this call sent no `files`, while `file_path` (
