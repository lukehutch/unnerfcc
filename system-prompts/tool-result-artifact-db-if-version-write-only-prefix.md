<!--
name: 'Tool Result: Artifact DB if_version Write Only Prefix'
description: Prefix explaining that if_version is only supported on write operations.
ccVersion: 2.1.270
-->
`if_version` only accepted with a write db_op (
