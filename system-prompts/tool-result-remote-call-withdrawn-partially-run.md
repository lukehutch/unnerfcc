<!--
name: 'Tool Result: Remote call withdrawn partially run'
description: Warning that call was withdrawn on remote machine and may have partially run.
ccVersion: 2.1.257
variables:
  - MACHINE_NAME
-->
s, and what became of the call could not be learned from it afterwards, so it was withdrawn there; it may have partially run. Check its effect on ${MACHINE_NAME} before repeating it.
