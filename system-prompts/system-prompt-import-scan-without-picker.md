<!--
name: 'System Prompt: /import scan without interactive picker'
description: >-
  Explains the user ran /import on a surface with no interactive picker, and
  presents what the configuration scan found.
ccVersion: 2.1.219
variables:
  - SCANNED_CONFIG_SOURCES
-->
The user ran `/import` on a surface without the interactive picker. A scan for ${SCANNED_CONFIG_SOURCES} config found the following.
