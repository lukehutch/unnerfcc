<!--
name: 'System Reminder: Thread message delivered to artifact editor worker'
description: >-
  Notice to the coordinator that a thread follow-up was delivered directly to an
  artifact editor worker.
ccVersion: 2.1.251
variables:
  - WORKER_REF
-->
 you just read (or will read next) was also delivered directly to the artifact editor worker ${WORKER_REF}, which is applying it now. Do not re-dispatch it. Reply with the link when that worker's NEXT result arrives; until then no_reply_needed (awaiting_worker_link).]
