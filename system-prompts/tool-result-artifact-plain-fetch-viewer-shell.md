<!--
name: 'Tool Result: Artifact plain fetch returns viewer shell'
description: >-
  Notifies that a claude.ai artifact cannot be fetched directly and returns only
  the viewer shell.
ccVersion: 2.1.251
-->
 is a claude.ai artifact. Its content is not fetchable from here — a plain fetch returns only the viewer shell.
