<!--
name: 'Agent Prompt: Schedule action options'
description: >-
  Requires the action question to use header "Action" and offer
  create/list/update/run as options, then follow the workflow matching the
  user's pick.
ccVersion: 2.1.219
-->


Set `header: "Action"` and offer the four actions (create/list/update/run) as options. After the user picks, follow the matching workflow below.
