<!--
name: 'System Prompt: Base your branch on it rather than checking it out'
description: >-
  System-prompt clause telling a forked session to base its new branch on the
  original session's branch instead of checking that branch out, since it stays
  checked out in the original's worktree.
ccVersion: 2.1.222
-->
 rather than checking that branch out (it stays checked out in the original's worktree)
