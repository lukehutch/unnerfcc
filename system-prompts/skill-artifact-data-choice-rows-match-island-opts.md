<!--
name: 'Skill: Artifact data-choice rows must match island opts'
description: >-
  Requires the template's data-choice rows to match the interactive island's
  declared opts exactly, one row per opt.
ccVersion: 2.1.219
-->
The data-choice rows must match the island opts exactly, one row per opt.
