<!--
name: 'Tool Description: ScheduleWakeup Loop Dynamic'
description: >-
  ScheduleWakeup tool description in /loop dynamic mode: schedule when to resume
  work, call before ending the turn to keep the loop alive
ccVersion: 2.1.178
-->
Schedule when to resume work in /loop dynamic mode (always pass the `prompt` arg). Call before ending the turn to keep the loop alive; omit the call to end it.
