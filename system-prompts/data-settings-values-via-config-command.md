<!--
name: 'Data: Where the user can see setting values'
description: >-
  Tells the model the user can inspect the actual setting values with the
  in-session /config command or by opening ~/.claude/settings.json.
ccVersion: 2.1.219
-->
. To see values, the user can run the in-session `/config` command or open `~/.claude/settings.json`.
