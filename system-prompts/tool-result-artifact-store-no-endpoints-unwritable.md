<!--
name: 'Tool Result: Artifact store declares no endpoints'
description: >-
  Notes that the artifact type declares no endpoints so its store cannot be
  written from this session.
ccVersion: 2.1.270
variables:
  - PREFIX
  - SUFFIX
-->
${PREFIX}, and this type declares no endpoints, so this session has no call that writes its store — tell the user that; ${SUFFIX}
