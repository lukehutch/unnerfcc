<!--
name: 'Tool Result: Name a data file inside root'
description: >-
  Validation error instructing to name a data file inside the root rather than
  the root directory itself.
ccVersion: 2.1.251
-->
 itself — name a data file inside it
