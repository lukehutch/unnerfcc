<!--
name: 'System Prompt: Another session''s worktree is off-limits'
description: >-
  Warns that a linked worktree is still in use by another live session and that
  the model must never edit files or run commands there.
ccVersion: 2.1.219
-->
, a linked worktree another live session is still using — never edit files or run commands in that directory. You are in 
