<!--
name: 'System Reminder: Prefer the scoped skill over the unscoped one'
description: >-
  Tells the model the skill is scoped to a directory and should be used instead
  of the unscoped variant of the same name.
ccVersion: 2.1.219
variables:
  - SKILL_SCOPE_DIRECTORY
-->
scoped to ${SKILL_SCOPE_DIRECTORY}/ — use this instead of the unscoped "
