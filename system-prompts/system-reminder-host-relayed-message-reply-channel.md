<!--
name: 'System Reminder: Reply to a host-relayed message through the host'
description: >-
  Tells the model the message came from the host application and its from= is a
  host session id SendMessage cannot reach, so any reply goes through the host's
  own messaging tool.
ccVersion: 2.1.251
-->
 This message was delivered by your host application, and its `from=` is a host session id that SendMessage cannot reach: reply through the host's own messaging tool with that id, if it provides one.
