<!--
name: 'Tool Result: Pass type or type_url exactly one'
description: >-
  Instructs the model to pass the artifact type as either `type` or `type_url`,
  exactly one.
ccVersion: 2.1.257
-->
pass the type as `type` (its name) or `type_url` (its link) — exactly one
