<!--
name: 'Tool Parameter: Artifact publish asset upload description'
description: >-
  Explains that publish with asset upload uploads the file to the artifact's
  asset store.
ccVersion: 2.1.277
-->
publish with `url`: true uploads `file_path` (or each of `file_paths`) to that artifact's asset store instead of publishing it as the page (see **Calls**).
