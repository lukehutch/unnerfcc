<!--
name: 'Tool Result: Teammates cannot spawn teammates'
description: >-
  Rejects a teammate spawn because the team roster is flat and tells the model
  to omit the name parameter to spawn a subagent instead.
ccVersion: 2.1.219
-->
Teammates cannot spawn other teammates — the team roster is flat. To spawn a subagent instead, omit the `name` parameter.
