<!--
name: 'Tool Result: Artifact copy saved afresh'
description: >-
  Informs the model that an artifact copy was modified after being saved so
  previous reads no longer count.
ccVersion: 2.1.251
variables:
  - SOURCE_PATH
-->
 (saved afresh: the copy at ${SOURCE_PATH} was modified after it was saved, so Reads of it no longer count)
