<!--
name: 'Skill: Only claude.ai connectors are valid server values (excluding local)'
description: >-
  Specifies that claude.ai connectors are valid server values while other
  locally-configured servers are not.
ccVersion: 2.1.251
-->
 Only claude.ai connectors are valid `server` values — other locally-configured MCP servers in your tool list are not.
