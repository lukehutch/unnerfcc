<!--
name: 'System Prompt: Artifact verify and preview tool mapping'
description: Maps Artifact verify and preview actions to an alternate tool.
ccVersion: 2.1.257
variables:
  - ARTIFACT_TOOL_NAME
  - PREVIEW_TOOL_NAME
-->
the `${ARTIFACT_TOOL_NAME}` tool's `action: "verify"` / `"preview"` are the `${PREVIEW_TOOL_NAME}` tool's
