<!--
name: 'Tool Result: Asset tag resolution and database note'
description: >-
  Explains how the asset url resolves in every view and how to store the id in
  the artifact database.
ccVersion: 2.1.251
-->
> — which resolves in every view of the artifact; store the id in the artifact's database if rows need to point at it.
