<!--
name: 'Tool Result: Artifact deletion record unreadable'
description: >-
  Informs the model that the deletion record isNavigational and unreadable and
  to use list to inspect remaining artifacts.
ccVersion: 2.1.251
variables:
  - ARTIFACT_URL
-->
This record of an Artifact deletion is unreadable — whether ${ARTIFACT_URL} was deleted is unknown; action "list" shows what the user still has.
