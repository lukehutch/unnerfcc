<!--
name: Tool-Call Outcome Annotation (Fail-Closed Fragment)
description: >-
  Fragment of the harness-authored tool-call outcome-annotation primer (variable
  F0p) that explains automode classifier failure modes (held back fail-closed =
  not a policy decision); injected into the model's system context.
ccVersion: 2.1.187
-->
and the call was held back fail-closed — also not a decision). A call with 
