<!--
name: 'Tool Result: Eval run url must name a stubbed artifact'
description: >-
  Tells the model this eval run stubs artifact publishing locally, so `url` may
  only name an eval-stub artifact this run published, and omitting it publishes
  fresh.
ccVersion: 2.1.235
-->
This eval run stubs artifact publishing locally: `url` may only name an artifact this run published (an eval-stub:// link), never a live claude.ai artifact. Omit `url` to publish fresh.
