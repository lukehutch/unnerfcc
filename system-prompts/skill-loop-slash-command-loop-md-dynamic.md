<!--
name: 'Skill: /loop with loop.md tasks (dynamic pacing)'
description: >-
  Handles a bare `/loop` invocation with no prompt and no interval when a
  loop-tasks file exists, running those tasks with model-chosen pacing.
ccVersion: 2.1.219
-->
# /loop — loop.md tasks with dynamic pacing

The user invoked `/loop` with no prompt and no interval and has a loop-tasks file at `
