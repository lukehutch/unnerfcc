<!--
name: 'Skill: Full settings JSON schema'
description: >-
  Embeds the full settings JSON schema in the config skill so the model can
  validate the keys it writes.
ccVersion: 2.1.219
variables:
  - SETTINGS_JSON_SCHEMA
-->


## Full Settings JSON Schema

```json
${SETTINGS_JSON_SCHEMA}
```
