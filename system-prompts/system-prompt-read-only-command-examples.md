<!--
name: 'System Prompt: Read-only command examples'
description: >-
  Lists example read-only commands (ls, git status, git log, git diff, find) the
  model may run freely.
ccVersion: 2.1.219
-->
ls, git status, git log, git diff, find
