<!--
name: 'Tool Result: Quickstart reads default design system README'
description: >-
  Notes that quickstart reads the default design system README unless approval
  is required.
ccVersion: 2.1.272
-->
 — and reads the README of the account's default design system, if there is one, skipped where that read would need approval
