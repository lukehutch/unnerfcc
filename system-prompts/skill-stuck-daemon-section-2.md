<!--
name: 'Skill: /stuck daemon section'
description: >-
  Daemon section of the /stuck slash command, describing what the background
  daemon manages and dumping daemon.lock, daemon.status.json, and the daemon
  log.
ccVersion: 2.1.219
-->
## Daemon

The background daemon manages `& <prompt>` jobs and `claude agents`. If the issue involves background sessions, look here.

### daemon.lock
```json
