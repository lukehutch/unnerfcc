<!--
name: 'System Reminder: Memory index in a store'
description: >-
  Names the memory index document and the store it lives in, interpolated into
  the memory-service reference-data wrapper and the memory instructions.
ccVersion: 2.1.231
variables:
  - MEMORY_INDEX_PATH
  - MEMORY_STORE_NAME
-->
the memory index `${MEMORY_INDEX_PATH}` in the `${MEMORY_STORE_NAME}` memory store
