<!--
name: 'Tool Parameter: Artifact database operations summary'
description: Summary of database delete and batch write actions and required parameters.
ccVersion: 2.1.270
-->
 'delete' with `collection` + `doc_id`; 'batch' with `writes`. Every action takes the artifact's `url`.
