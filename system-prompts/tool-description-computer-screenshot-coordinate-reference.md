<!--
name: 'Tool Description: Computer screenshot coordinate reference'
description: >-
  Notes error on empty allowlist and that subsequent click coordinates are
  relative to returned screenshot.
ccVersion: 2.1.251
variables:
  - ADDITIONAL_SCREENSHOT_NOTES
-->
 Returns an error if the allowlist is empty. The returned image is what subsequent click coordinates are relative to.${ADDITIONAL_SCREENSHOT_NOTES}
