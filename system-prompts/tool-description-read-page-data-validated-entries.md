<!--
name: 'Tool Description: read_page_data returns validated entries only'
description: >-
  Explains the page-data reader returns only the island's validated typed
  entries — never page content — and refuses when the island is out of contract.
ccVersion: 2.1.219
-->
), and returns its validated typed entries only — never page content; it refuses when the island is out of contract.
