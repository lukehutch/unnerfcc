<!--
name: 'System Reminder: This session paused comment replies'
description: >-
  Informs that another live session claimed replies and this session paused its
  own at that session's request.
ccVersion: 2.1.251
variables:
  - ARTIFACT_ID
-->
Another live session of this same conversation claimed the replies to comments on ${ARTIFACT_ID} a moment after this one; this session paused its own at that session's request, so only that session answers them now. Nothing to do — a publish the user asks for here takes them back; do not republish or stop a watch on your own.
