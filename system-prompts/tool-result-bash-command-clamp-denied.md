<!--
name: 'Tool Result: Bash command clamp denied the command'
description: >-
  Tells the agent its Bash use is clamped to a fixed set of per-spawn command
  forms and this command was denied.
ccVersion: 2.1.231
-->
 has been denied: this agent's Bash use is clamped to a fixed set of command forms (per-spawn bashCommandClamp), and 
