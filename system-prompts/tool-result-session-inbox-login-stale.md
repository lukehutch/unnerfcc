<!--
name: 'Tool Result: Session inbox login stale'
description: >-
  Informs the model that the session inbox read failed because the device login
  is stale.
ccVersion: 2.1.273
-->
The session service refused the read because the login on this device is stale (run /login again). Tell the user; do not retry.
