<!--
name: 'Tool Result: Cloud session cannot deliver to other sessions'
description: >-
  Tells the model this cloud session's credential is accepted for its own work
  but not for delivering to another session, so it cannot reply from here and
  should say so instead of retrying.
ccVersion: 2.1.232
-->
this cloud session cannot message other sessions yet — its credential is accepted for its own work but not for delivering to another session, so a reply from here is not possible; say so in your response instead of retrying
