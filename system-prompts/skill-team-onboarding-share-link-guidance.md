<!--
name: 'Skill: Team onboarding share link guidance'
description: >-
  Tells the model to share the existing onboarding guide link and to have the
  user run /team-onboarding themselves, since the model cannot invoke it.
ccVersion: 2.1.219
-->
). If this link is what the user needed, share it. If they want to create or update a guide, tell them to run /team-onboarding themselves (it scans local session data and cannot be invoked by the model).
