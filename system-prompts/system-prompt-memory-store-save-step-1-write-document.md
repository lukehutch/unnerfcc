<!--
name: 'System Prompt: Memory store save Step 1 (write the document)'
description: >-
  First step of the memory-store save procedure — save the memory as its own
  document in the store with the memory write tool, using the frontmatter format
  that follows.
ccVersion: 2.1.231
variables:
  - MEMORY_WRITE_TOOL_NAME
-->
**Step 1** — save the memory as its own document in the store with ${MEMORY_WRITE_TOOL_NAME}, using this frontmatter format:
