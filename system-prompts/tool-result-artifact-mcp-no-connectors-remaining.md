<!--
name: 'Tool Result: Artifact no connectors remaining'
description: >-
  Informs that no declared servers remained so the published artifact has no
  connector access and stored declarations are replaced.
ccVersion: 2.1.272
-->
, and that the page now has no connector access at all (no declared server remained, so this publish carries no "mcp" and any stored "mcp" declaration is replaced)
