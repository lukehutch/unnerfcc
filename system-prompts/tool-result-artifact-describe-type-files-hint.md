<!--
name: 'Tool Result: Describe type files hint'
description: Mentions using the action to view an artifact type's files first if needed.
ccVersion: 2.1.270
variables:
  - ACTION_NAME
-->
 ${ACTION_NAME} with a `type_url` shows a type's files first if you need them.
