<!--
name: 'Tool Result: Open artifact after script write hint'
description: >-
  Advises calling action open with the artifact URL so the user sees it after a
  script write.
ccVersion: 2.1.263
-->
, then pass `action: "open"` with its `url` so the user sees it (a script write does not open it)
