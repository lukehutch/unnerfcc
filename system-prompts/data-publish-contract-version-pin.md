<!--
name: 'Data: Published contract version pin'
description: >-
  Footnote naming the contract version the target currently runs and noting that
  a carry-forward republish keeps that pin.
ccVersion: 2.1.219
variables:
  - TARGET_DESCRIPTOR
-->
 — the contract the target${TARGET_DESCRIPTOR} currently runs. A carry-forward republish keeps this pin._
