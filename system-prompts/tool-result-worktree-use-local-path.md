<!--
name: 'Tool Result: Address worktree files by their local path'
description: >-
  Tells the model a file genuinely inside the worktree must be addressed by its
  local, plainly-spelled path.
ccVersion: 2.1.219
variables:
  - WORKTREE_PATH
-->
If the file is genuinely inside the worktree ${WORKTREE_PATH}, address it by its local, plainly-spelled path instead.
