<!--
name: 'System Reminder: Directory sync environment recreated empty (reason)'
description: >-
  Injected reason clause explaining that directory sync failed because the
  environment was recreated empty while incremental changes were sent.
ccVersion: 2.1.251
-->
this environment was recreated empty and the user's machine only sends changes on top of what the earlier one had
