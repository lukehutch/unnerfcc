<!--
name: 'Tool Result: Artifact Publish Favicon Read Failed'
description: >-
  Instructs the model to supply a favicon explicitly when reading the existing
  favicon failed.
ccVersion: 2.1.270
-->
couldn't read this Artifact's current favicon to keep it, so nothing was published — pass `favicon` explicitly: the emoji it already has (an artifact listing shows it, or the user can tell you); if it has none, choose one (it will then stay)
