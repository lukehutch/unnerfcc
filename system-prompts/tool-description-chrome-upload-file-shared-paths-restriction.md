<!--
name: 'Tool Description: Chrome upload file shared paths restriction'
description: >-
  Notes that only files shared with the session can be uploaded via the browser
  file upload tool.
ccVersion: 2.1.270
variables:
  - PREFIX
  - SUFFIX
-->
${PREFIX} Only files the user has shared with this session (attachments, the session's outputs/uploads folders, or folders the user has connected) can be uploaded; other paths will be rejected. ${SUFFIX}
