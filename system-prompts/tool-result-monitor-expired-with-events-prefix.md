<!--
name: 'Tool Result: Monitor expired with events (prefix)'
description: >-
  Prefix reporting that a background monitor expired after a duration with a
  count of delivered events.
ccVersion: 2.1.270
variables:
  - TIMEOUT
  - EVENT_COUNT
-->
[Monitor expired after ${TIMEOUT} with ${EVENT_COUNT} 
