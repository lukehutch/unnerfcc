<!--
name: 'Tool Result: Copying HTML SVG XML documents disallowed'
description: >-
  Validation error explaining that copying HTML, SVG, or XML files across
  artifacts is forbidden.
ccVersion: 2.1.263
-->
 would copy an HTML, SVG or XML document from another Artifact, which is never allowed — read it with action "read_file" and publish it as your own file
