<!--
name: 'Tool Parameter: Reference file via command guidance'
description: Instructions on referencing files via path in tool invocations.
ccVersion: 2.1.251
variables:
  - TOOL_NAME
  - COMMAND
-->
use ${TOOL_NAME} to "${COMMAND}" and reference the file as @<path>
