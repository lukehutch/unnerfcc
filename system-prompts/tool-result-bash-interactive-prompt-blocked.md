<!--
name: 'Tool Result: Command blocked on interactive prompt'
description: >-
  Tells the model a command is likely waiting on an interactive prompt and to
  stop and re-run with piped input or a non-interactive flag.
ccVersion: 2.1.219
-->


The command is likely blocked on an interactive prompt. Stop this task and re-run with piped input (e.g., `echo y | command`) or a non-interactive flag if one exists.
