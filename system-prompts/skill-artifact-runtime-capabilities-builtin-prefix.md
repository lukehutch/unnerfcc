<!--
name: 'Skill: Built-in capabilities on every page prefix'
description: >-
  Prefix listing capabilities built into every page that must never be declared
  in capabilities.
ccVersion: 2.1.251
-->
built in on every page, called without declaring (never pass these in `capabilities`): 
