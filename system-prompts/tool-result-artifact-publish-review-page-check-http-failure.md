<!--
name: 'Tool Result: Publish refused review page check HTTP failure'
description: >-
  Publish refusal explaining non-transient HTTP failures and advising action:
  read or fresh publish.
ccVersion: 2.1.251
-->
 failure is not transient, so retrying this publish cannot succeed — read the page (action: "read") to check its state, or publish a fresh artifact (omit `url` and use a new `file_path`).
