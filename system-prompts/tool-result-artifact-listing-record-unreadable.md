<!--
name: 'Tool Result: Listing record unreadable'
description: >-
  Informs the model that the listing record is unreadable and instructs it to
  rerun list with type or type_url.
ccVersion: 2.1.257
-->
This record of the listing is unreadable — run action "list" with the `type` or `type_url` again for the real one.
