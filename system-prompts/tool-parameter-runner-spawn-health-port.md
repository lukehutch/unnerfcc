<!--
name: 'Tool Parameter: Self-hosted runner spawn health_port'
description: >-
  inputSchema description for the spawn-runner tool's health_port flag, giving
  its default and that 0 disables /healthz.
ccVersion: 2.1.231
variables:
  - DEFAULT_HEALTH_PORT
-->
Default ${DEFAULT_HEALTH_PORT}. 0 disables /healthz.
