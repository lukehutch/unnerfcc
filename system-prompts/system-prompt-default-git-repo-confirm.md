<!--
name: 'System Prompt: Confirm the default git repo'
description: >-
  Tells the model a default git repo is already configured and to ask the user
  whether it is the right one before proceeding.
ccVersion: 2.1.219
variables:
  - DEFAULT_GIT_REPO
-->
 The default git repo is already set to `${DEFAULT_GIT_REPO}`. Ask the user if this is the right repo or if they need a different one.
