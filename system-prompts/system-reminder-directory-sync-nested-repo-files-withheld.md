<!--
name: 'System Reminder: Directory sync nested repo files withheld'
description: >-
  Informs the model that files inside nested repositories do not sync to the
  user machine.
ccVersion: 2.1.251
variables:
  - SUGGESTION
-->
 reaches the user's machine (the user is told as well). If the user needs those files on their machine, tell them they exist only here${SUGGESTION}.
