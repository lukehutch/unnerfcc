<!--
name: 'Tool Result: Type create result connector instructions'
description: >-
  Explains that the create result provides instructions on filling the document
  via the connector rather than publishing a page.
ccVersion: 2.1.277
variables:
  - CREATE_PARAMS
-->
, ${CREATE_PARAMS}. The create result carries the type's instructions and says how to fill the document through the connector, not by publishing a page.
