<!--
name: 'Tool Result: Session registry unreadable transient error'
description: >-
  Informs the model that the local session registry is temporarily unreadable
  and to retry.
ccVersion: 2.1.251
-->
 — this machine's session registry could not be read just now (a transient local condition). Retry the same name shortly.
