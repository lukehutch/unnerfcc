<!--
name: 'Data: Artifact publish files parameters shorthand'
description: Specifies shorthand publish parameters with index file_path and project files.
ccVersion: 2.1.273
-->
, `root`: that folder, `file_path`: the index's absolute path, `files`: the rest by their `project/…` paths)
