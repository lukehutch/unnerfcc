<!--
name: 'Skill: Design sync README header'
description: Header template for design system documentation bundled as a browser global.
ccVersion: 2.1.251
variables:
  - DESIGN_SYSTEM_NAME
  - PACKAGE_NAME
  - PACKAGE_VERSION
-->
# ${DESIGN_SYSTEM_NAME} (${PACKAGE_NAME}@${PACKAGE_VERSION})

This design system is the published ${PACKAGE_NAME} React library, bundled as a single
browser global. All 
