<!--
name: 'Data: Shell command counts (opted in at Q2)'
description: >-
  Notes the user opted in at Q2, that only command words were read, and that the
  counts should be merged with the per-project section above.
ccVersion: 2.1.219
-->

The user opted into this at Q2. Raw command lines were never read into the transcript — only the command words above. Merge these with the per-project counts in the section above.
