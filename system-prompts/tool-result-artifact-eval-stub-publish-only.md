<!--
name: 'Tool Result: Eval run supports publishing only'
description: >-
  Tells the model this eval run stubs artifact publishing locally and has no
  artifact control plane: only publishing (the default action) is available here
  — list, comments, replies, watches, database writes, asset uploads, and live
  docs are not.
ccVersion: 2.1.251
-->
This eval run stubs artifact publishing locally and has no artifact control plane: only publishing (the default action) is available here — list, delete, comments, replies, watches, database writes, asset uploads, and live docs are not.
