<!--
name: 'Agent Prompt: Security monitor wake envelope provenance clause'
description: >-
  Clause identifying valid server wake envelopes with human provenance on the
  triggering message.
ccVersion: 2.1.263
-->
that is the server's `<wake>` envelope with `from="human"` on its triggering `<message>`
