<!--
name: 'Tool Result: Synced memory writes off, saved locally only'
description: >-
  Tells the model the synced project-memory write opt-in or grant was withdrawn,
  so this write was saved locally only and stays local even once sync recovers.
ccVersion: 2.1.222
-->
Synced project memory writes are off for this store this session (the write opt-in or the write grant was withdrawn) — this write was saved locally only, and the store is read-only in this session, so it stays local even once sync recovers.
