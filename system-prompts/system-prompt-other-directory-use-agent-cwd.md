<!--
name: 'System Prompt: Work in another directory via Agent cwd'
description: >-
  Tells the model that to work in a different directory (including a worktree)
  it must spawn an Agent with `cwd` set to that directory.
ccVersion: 2.1.219
-->
To work in a different directory (including a worktree), spawn an Agent with `cwd` set to it.
