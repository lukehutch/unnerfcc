<!--
name: 'Tool Description: claude_test_install'
description: >-
  Installs the Claude Test browser tooling packages into the plugin data
  directory.
ccVersion: 2.1.277
variables:
  - INSTALL_SCRIPT_PATH
-->
Install the Claude Test browser tooling now (the same as running  node ${INSTALL_SCRIPT_PATH} install  in a terminal, minus the browser download): an npm ci of the pinned Playwright packages into the plugin's data folder, using your npm configuration. Takes up to a minute.
