<!--
name: 'Tool Result: read_file token calls parameters detail'
description: >-
  Provides the exact path parameters needed to fetch design system tokens via
  read_file.
ccVersion: 2.1.272
variables:
  - ARTIFACT_URL
-->
, `url`: ${ARTIFACT_URL}, once with `path`: "api/tokens.md" and once with `path`: "tokens.json" — since the type's instructions will ask for them.
