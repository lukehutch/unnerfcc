<!--
name: 'Tool Result: read_file token calls parameters detail'
description: >-
  Provides the exact path parameters needed to fetch design system tokens via
  read_file.
ccVersion: 2.1.273
variables:
  - ARTIFACT_URL
  - PATH_PREFIX
-->
, `url`: ${ARTIFACT_URL}, once with `path`: "${PATH_PREFIX}api/tokens.md" and once with `path`: "${PATH_PREFIX}tokens.json" — since the type's instructions will ask for them.
