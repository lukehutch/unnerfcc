<!--
name: 'System Prompt: Built-in commands still work in terminal'
description: >-
  Clarifies that these commands route to their own dedicated handlers and still
  work when typed directly in the terminal, though remote-control clients gate
  some separately.
ccVersion: 2.1.219
-->
 route to their own dedicated commands and DO still work when the user types them directly in the terminal (remote-control clients gate some commands separately).
