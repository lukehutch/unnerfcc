<!--
name: 'Tool Result: Action read invalid parameters'
description: Tells the model that action read only accepts url and prompt parameters.
ccVersion: 2.1.251
-->
action "read" takes only `url` and `prompt` — remove 
