<!--
name: 'Tool Result: Artifact comments resumed from a cursor'
description: >-
  Tail of the artifact comments listing telling the model that the threads at or
  before the cursor were skipped and to re-run without `cursor` for the full
  list.
ccVersion: 2.1.231
-->
 skipped — re-run without `cursor` for the full list.
