<!--
name: >-
  Tool Result: Auto-replies not resumed — approval did not cover the recorded
  stop
description: >-
  Tells the model the approval did not cover the stop now recorded — a stop
  landed while the approval was pending, or the approval arrived without its
  consent-card record — so it should ask the user before calling resume_replies
  again.
ccVersion: 2.1.235
-->
Auto-replies were NOT resumed: the approval did not cover the stop that is now recorded — either a stop landed while the approval was pending, or the approval arrived without its consent-card record. Ask the user, and call resume_replies again only if they still want auto-replies resumed.
